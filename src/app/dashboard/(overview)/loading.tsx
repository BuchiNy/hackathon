//shimmer effects skeletons to show while the page loads the ui/data
import DashboardSkeleton from "../libs/components/skeletons"

export default function Loading(){
    return <DashboardSkeleton />;
}